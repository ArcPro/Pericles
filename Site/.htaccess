Options -Indexes

<FilesMatch "^(\.env(?:\..*)?|composer\.(json|lock)|phpunit\.xml|bootstrap\.php)$">
    Require all denied
</FilesMatch>

RewriteEngine On
RewriteRule ^(bin|config|migrations|src|storage|tests|vendor)(/|$) - [F,L,NC]
